# Example: Secrets Management
#
# This example demonstrates how to use `env_var` to safely handle secrets.
# The secret is read from the environment and never hardcoded.
#
# Usage:
#   export API_KEY="secret-123"
#   avon eval examples/secrets_test.av

let api_key = env_var "API_KEY" in
@/config.yml {"
api_key: {api_key}
debug: false
"}
